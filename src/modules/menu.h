#ifndef GYMTRAC_MENU_H
#define GYMTRAC_MENU_H

/**
 * Runs the top-level login and registration loop.
 *
 * Shows the unauthenticated menu, handles login and self-registration,
 * and dispatches to the role-specific menu after a successful login.
 */
void run_main_menu();

#endif
